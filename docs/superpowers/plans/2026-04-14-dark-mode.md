# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a user-controlled dark mode toggle to FuelHunter that persists in `localStorage` and defaults to light mode.

**Architecture:** Tailwind `darkMode: 'class'` drives all styling via `dark:` variants. An Alpine.js store (registered via `alpine:init`, since Alpine is bundled with Livewire 3) holds and toggles the dark state, applying the `dark` class to `<html>` imperatively. A synchronous inline script prevents flash-of-unstyled-content on page load. Chart.js charts update via a `MutationObserver` that watches the `dark` class on `<html>`.

**Tech Stack:** Tailwind CSS v3 (darkMode class), Alpine.js (via Livewire 3 bundle), Chart.js 4, Laravel Blade

---

### Task 1: Enable Tailwind dark mode

**Files:**
- Modify: `tailwind.config.js`

- [ ] **Step 1: Add `darkMode: 'class'`**

Replace the contents of `tailwind.config.js` with:

```js
/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./resources/**/*.blade.php",
        "./resources/**/*.js",
        "./resources/**/*.vue",
    ],
    darkMode: 'class',
    theme: {
        extend: {},
    },
    plugins: [],
}
```

- [ ] **Step 2: Verify build succeeds**

```bash
npm run build
```

Expected: build completes with no errors. Dark mode variants are now generated.

- [ ] **Step 3: Commit**

```bash
git add tailwind.config.js
git commit -m "feat: enable Tailwind class-based dark mode"
```

---

### Task 2: Alpine store + FOUC prevention

**Files:**
- Modify: `resources/views/components/layout.blade.php`

Alpine is bundled with Livewire 3 — no npm install needed. The store is registered via `alpine:init`. A synchronous inline script sets the `dark` class before Alpine or any CSS loads, preventing the white flash on load.

- [ ] **Step 1: Add FOUC prevention script and Alpine store to `<head>`**

In `resources/views/components/layout.blade.php`, replace the `<head>` open tag and its contents up to (and including) the `@vite` line:

```html
<!doctype html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>FuelHunter</title>
    {{-- FOUC prevention: set dark class synchronously before any paint --}}
    <script>
        if (localStorage.getItem('theme') === 'dark') {
            document.documentElement.classList.add('dark');
        }
    </script>
    @vite('resources/css/app.css')
    {{-- Alpine theme store (Alpine is bundled with Livewire 3) --}}
    <script>
        document.addEventListener('alpine:init', () => {
            Alpine.store('theme', {
                dark: localStorage.getItem('theme') === 'dark',
                toggle() {
                    this.dark = !this.dark;
                    localStorage.setItem('theme', this.dark ? 'dark' : 'light');
                    document.documentElement.classList.toggle('dark', this.dark);
                }
            });
        });
    </script>
</head>
```

- [ ] **Step 2: Add dark background to `<body>`**

Change the `<body>` tag from:
```html
<body class="h-full bg-slate-50">
```
to:
```html
<body class="h-full bg-slate-50 dark:bg-slate-950 transition-colors duration-200">
```

- [ ] **Step 3: Verify in browser**

Start the app (`php artisan serve --port=7050`), open DevTools console, run:
```js
localStorage.setItem('theme', 'dark'); location.reload();
```
Expected: page reloads with `dark` class on `<html>` before any flash. Body background turns dark.

Run `localStorage.setItem('theme', 'light'); location.reload()` to restore.

- [ ] **Step 4: Commit**

```bash
git add resources/views/components/layout.blade.php
git commit -m "feat: add Alpine theme store and FOUC prevention"
```

---

### Task 3: Toggle buttons in layout + nav dark variants

**Files:**
- Modify: `resources/views/components/layout.blade.php`

- [ ] **Step 1: Add desktop toggle button (right of nav links)**

In `layout.blade.php`, find the desktop nav links block:
```html
{{-- Desktop nav links — absolutely centred --}}
<div class="hidden md:flex absolute inset-0 items-center justify-center pointer-events-none">
    <div class="flex items-center gap-1 pointer-events-auto">
        <x-nav-link href="/"          :active="request()->is('/')">Map</x-nav-link>
        <x-nav-link href="/fuel"      :active="request()->is('fuel*')">Fuel Sites</x-nav-link>
        <x-nav-link href="/dashboard" :active="request()->is('dashboard')">Statistics</x-nav-link>
        <x-nav-link href="/about"     :active="request()->is('about')">About</x-nav-link>
    </div>
</div>
```

Replace with:
```html
{{-- Desktop nav links — absolutely centred --}}
<div class="hidden md:flex absolute inset-0 items-center justify-center pointer-events-none">
    <div class="flex items-center gap-1 pointer-events-auto">
        <x-nav-link href="/"          :active="request()->is('/')">Map</x-nav-link>
        <x-nav-link href="/fuel"      :active="request()->is('fuel*')">Fuel Sites</x-nav-link>
        <x-nav-link href="/dashboard" :active="request()->is('dashboard')">Statistics</x-nav-link>
        <x-nav-link href="/about"     :active="request()->is('about')">About</x-nav-link>
        <button @click="$store.theme.toggle()"
                class="w-8 h-8 rounded-lg border flex items-center justify-center ml-1
                       transition-colors duration-150
                       border-slate-200 bg-slate-50 hover:bg-slate-100
                       dark:border-slate-700 dark:bg-slate-800 dark:hover:bg-slate-700">
            {{-- Sun: shown in light mode --}}
            <svg x-show="!$store.theme.dark" class="w-4 h-4 text-slate-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
                <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
                <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
                <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
            </svg>
            {{-- Moon: shown in dark mode --}}
            <svg x-show="$store.theme.dark" class="w-4 h-4 text-slate-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z"/>
            </svg>
        </button>
    </div>
</div>
```

- [ ] **Step 2: Add dark variants to the desktop nav bar**

Change the `<nav>` element from:
```html
<nav class="bg-white/80 backdrop-blur-md border-b border-slate-900/[0.07] sticky top-0 z-50">
```
to:
```html
<nav class="bg-white/80 dark:bg-slate-900/80 backdrop-blur-md border-b border-slate-900/[0.07] dark:border-slate-700/50 sticky top-0 z-50">
```

- [ ] **Step 3: Add dark mode toggle row to mobile More overlay**

Find the More overlay content in `layout.blade.php`:
```html
<div x-show="moreOpen"
     ...
     class="absolute bottom-full right-0 mb-1 mr-2 bg-white border border-slate-200 rounded-2xl shadow-xl overflow-hidden w-44"
     style="display: none;">
    <a href="/about"
       @click="moreOpen = false"
       class="flex items-center gap-3 px-4 py-3 text-sm font-medium
              {{ request()->is('about') ? 'text-indigo-600 bg-indigo-50' : 'text-slate-700 hover:bg-slate-50' }}
              transition-colors duration-150">
```

Replace with:
```html
<div x-show="moreOpen"
     x-transition:enter="transition ease-out duration-150"
     x-transition:enter-start="opacity-0 translate-y-2"
     x-transition:enter-end="opacity-100 translate-y-0"
     x-transition:leave="transition ease-in duration-100"
     x-transition:leave-start="opacity-100 translate-y-0"
     x-transition:leave-end="opacity-0 translate-y-2"
     @click.away="moreOpen = false"
     class="absolute bottom-full right-0 mb-1 mr-2 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl shadow-xl overflow-hidden w-52"
     style="display: none;">

    {{-- Dark mode toggle --}}
    <button @click="$store.theme.toggle()"
            class="flex items-center justify-between w-full px-4 py-3 border-b border-slate-100 dark:border-slate-700
                   text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors duration-150">
        <div class="flex items-center gap-3">
            <svg x-show="!$store.theme.dark" class="w-4 h-4 text-slate-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
                <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
                <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
                <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
            </svg>
            <svg x-show="$store.theme.dark" class="w-4 h-4 text-slate-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z"/>
            </svg>
            <span class="text-sm font-medium">Dark mode</span>
        </div>
        {{-- Pill toggle --}}
        <div :class="$store.theme.dark ? 'bg-indigo-600' : 'bg-slate-200'"
             class="w-9 h-5 rounded-full relative transition-colors duration-200 flex-shrink-0">
            <div :class="$store.theme.dark ? 'translate-x-4' : 'translate-x-0'"
                 class="w-4 h-4 bg-white rounded-full absolute top-0.5 left-0.5 transition-transform duration-200 shadow-sm"></div>
        </div>
    </button>

    <a href="/about"
       @click="moreOpen = false"
       class="flex items-center gap-3 px-4 py-3 text-sm font-medium
              {{ request()->is('about') ? 'text-indigo-600 bg-indigo-50 dark:bg-indigo-950 dark:text-indigo-400' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800' }}
              transition-colors duration-150">
```

- [ ] **Step 4: Add dark variants to mobile bottom tab bar**

Change the mobile nav `<nav>` from:
```html
<nav x-data="{ moreOpen: false }"
     class="md:hidden fixed bottom-0 inset-x-0 z-50 bg-white/90 backdrop-blur-md border-t border-slate-900/[0.07]"
```
to:
```html
<nav x-data="{ moreOpen: false }"
     class="md:hidden fixed bottom-0 inset-x-0 z-50 bg-white/90 dark:bg-slate-900/90 backdrop-blur-md border-t border-slate-900/[0.07] dark:border-slate-700/50"
```

- [ ] **Step 5: Add dark variants to the page heading header**

Change:
```html
<header class="bg-white border-b border-gray-100 shadow-sm">
    <div class="mx-auto max-w-7xl px-4 py-3 sm:py-5 sm:px-6 lg:px-8">
        <h1 class="text-xl sm:text-2xl font-bold tracking-tight text-gray-900">{{ $heading }}</h1>
```
to:
```html
<header class="bg-white dark:bg-slate-900 border-b border-gray-100 dark:border-slate-700 shadow-sm">
    <div class="mx-auto max-w-7xl px-4 py-3 sm:py-5 sm:px-6 lg:px-8">
        <h1 class="text-xl sm:text-2xl font-bold tracking-tight text-gray-900 dark:text-slate-100">{{ $heading }}</h1>
```

- [ ] **Step 6: Verify toggle works in browser**

Open `http://localhost:7050`. Click the sun icon next to "About" in the desktop nav. Expect:
- Background turns dark (`#0f172a`)
- Nav turns dark
- Icon swaps to moon
- Reload — stays dark (persisted in localStorage)

- [ ] **Step 7: Commit**

```bash
git add resources/views/components/layout.blade.php
git commit -m "feat: add dark mode toggle buttons and nav dark variants"
```

---

### Task 4: Nav-link component dark variants

**Files:**
- Modify: `resources/views/components/nav-link.blade.php`

- [ ] **Step 1: Add dark variants**

Replace the entire file with:

```blade
@props(['active' => false])

<a {{ $attributes }} class="{{ $active
    ? 'bg-indigo-50 dark:bg-indigo-950 text-indigo-600 dark:text-indigo-400 font-semibold'
    : 'text-slate-500 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-100 hover:bg-slate-100 dark:hover:bg-slate-800'
}} rounded-lg px-3 py-2 text-sm transition-colors duration-150">
    {{ $slot }}
</a>
```

- [ ] **Step 2: Verify in browser**

Toggle dark mode. Active nav link should show indigo on dark background. Inactive links should be visible.

- [ ] **Step 3: Commit**

```bash
git add resources/views/components/nav-link.blade.php
git commit -m "feat: dark variants on nav-link component"
```

---

### Task 5: Dashboard blade dark variants (static elements)

**Files:**
- Modify: `resources/views/livewire/dashboard.blade.php`

This task covers only the Blade/HTML sections — no JS changes. Go class by class through the file.

- [ ] **Step 1: Stat cards — add dark variants**

The three stat cards at the top each use `bg-white rounded-2xl p-5 border border-slate-200 shadow-sm`. Apply dark variants to each card's wrapper and all text inside.

For the **Avg Price card** (line ~38), change:
```html
<div class="dash-card bg-white rounded-2xl p-5 border border-slate-200 shadow-sm border-t-[3px] border-t-indigo-500 relative overflow-hidden">
    <div class="flex items-center gap-2 mb-3">
        <div class="w-7 h-7 rounded-lg bg-indigo-50 flex items-center justify-center">
```
to:
```html
<div class="dash-card bg-white dark:bg-slate-900 rounded-2xl p-5 border border-slate-200 dark:border-slate-700 shadow-sm border-t-[3px] border-t-indigo-500 relative overflow-hidden">
    <div class="flex items-center gap-2 mb-3">
        <div class="w-7 h-7 rounded-lg bg-indigo-50 dark:bg-indigo-950 flex items-center justify-center">
```

And change the text inside that card:
```html
<p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">
```
to:
```html
<p class="text-[10px] font-bold text-slate-400 dark:text-slate-500 uppercase tracking-widest">
```

```html
<p class="text-[2.2rem] font-bold text-slate-900 leading-none tracking-tight">
    ${{ number_format($stats['avg_price'], 3) }}<span class="text-lg text-slate-400 font-medium ml-1.5">/L</span>
</p>
<p class="text-xs text-slate-500 mt-2">Current average · all QLD sites</p>
...
<p class="text-2xl font-bold text-slate-400">—</p>
<p class="text-xs text-slate-500 mt-2">Select a fuel type above</p>
```
to:
```html
<p class="text-[2.2rem] font-bold text-slate-900 dark:text-slate-100 leading-none tracking-tight">
    ${{ number_format($stats['avg_price'], 3) }}<span class="text-lg text-slate-400 dark:text-slate-500 font-medium ml-1.5">/L</span>
</p>
<p class="text-xs text-slate-500 dark:text-slate-400 mt-2">Current average · all QLD sites</p>
...
<p class="text-2xl font-bold text-slate-400 dark:text-slate-500">—</p>
<p class="text-xs text-slate-500 dark:text-slate-400 mt-2">Select a fuel type above</p>
```

Apply the same pattern to the **Sites Reporting card** (sky accent) and **Date Range card** (amber accent) — same wrapper class changes, same text changes.

- [ ] **Step 2: Filter card dark variants**

The filter card wrapper:
```html
<div class="dash-card bg-white rounded-2xl border border-gray-100 shadow-sm p-5">
```
becomes:
```html
<div class="dash-card bg-white dark:bg-slate-900 rounded-2xl border border-gray-100 dark:border-slate-700 shadow-sm p-5">
```

Filter labels:
```html
<label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-2">Date Range</label>
```
becomes:
```html
<label class="block text-[10px] font-bold text-gray-400 dark:text-slate-500 uppercase tracking-widest mb-2">Date Range</label>
```
(apply to both "Date Range" and "Fuel Types" labels)

Date preset buttons (inactive state):
```html
'border-gray-200 bg-gray-50 text-gray-500 hover:border-indigo-300 hover:text-indigo-500 hover:bg-indigo-50'
```
becomes:
```html
'border-gray-200 dark:border-slate-600 bg-gray-50 dark:bg-slate-800 text-gray-500 dark:text-slate-400 hover:border-indigo-300 hover:text-indigo-500 hover:bg-indigo-50'
```

Date inputs:
```html
class="border border-gray-200 rounded-xl px-3 py-2 text-sm bg-gray-50
       focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-transparent
       transition-shadow duration-150"
```
becomes:
```html
class="border border-gray-200 dark:border-slate-600 rounded-xl px-3 py-2 text-sm bg-gray-50 dark:bg-slate-800
       text-gray-900 dark:text-slate-100
       focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-transparent
       transition-shadow duration-150"
```

Fuel type checkboxes (inactive state):
```html
'border-gray-200 bg-gray-50 text-gray-500 hover:border-indigo-300 hover:text-indigo-500 hover:bg-indigo-50'
```
becomes:
```html
'border-gray-200 dark:border-slate-600 bg-gray-50 dark:bg-slate-800 text-gray-500 dark:text-slate-400 hover:border-indigo-300 hover:text-indigo-500 hover:bg-indigo-50'
```

- [ ] **Step 3: Chart card dark variants**

The chart card wrapper:
```html
<div class="dash-card bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
```
becomes:
```html
<div class="dash-card bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-700 shadow-sm overflow-hidden">
```

Chart card headings:
```html
<h2 class="text-slate-900 text-xl font-bold tracking-tight">Average Fuel Price</h2>
<p class="text-slate-500 text-sm mt-0.5">Cents per litre · Queensland</p>
```
becomes:
```html
<h2 class="text-slate-900 dark:text-slate-100 text-xl font-bold tracking-tight">Average Fuel Price</h2>
<p class="text-slate-500 dark:text-slate-400 text-sm mt-0.5">Cents per litre · Queensland</p>
```

Live Data badge:
```html
<div class="flex items-center gap-2 bg-slate-50 rounded-xl px-3 py-1.5 border border-slate-200">
    ...
    <span class="text-xs text-slate-600 font-semibold">Live Data</span>
```
becomes:
```html
<div class="flex items-center gap-2 bg-slate-50 dark:bg-slate-800 rounded-xl px-3 py-1.5 border border-slate-200 dark:border-slate-700">
    ...
    <span class="text-xs text-slate-600 dark:text-slate-400 font-semibold">Live Data</span>
```

- [ ] **Step 4: Brand Market Share card dark variants**

Apply same wrapper pattern: `bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700`.

Change heading/subtitle:
```html
<h2 class="text-slate-900 text-xl font-bold tracking-tight">Brand Market Share</h2>
<p class="text-slate-500 text-sm mt-0.5">Share of total Queensland fuel sites by brand</p>
```
to:
```html
<h2 class="text-slate-900 dark:text-slate-100 text-xl font-bold tracking-tight">Brand Market Share</h2>
<p class="text-slate-500 dark:text-slate-400 text-sm mt-0.5">Share of total Queensland fuel sites by brand</p>
```

Brand share legend `<div id="brandShareLegend" ...>` — its content is generated in JS. That gets handled in Task 6.

- [ ] **Step 5: Global Oil Prices card dark variants**

Find the oil prices card (starts with `<div class="dash-card bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">`). Apply:
- Wrapper: add `dark:bg-slate-900 dark:border-slate-700`
- `<h2 class="text-slate-900 ...">`: add `dark:text-slate-100`
- `<p class="text-slate-500 ...">` (subtitle "USD — last 72 hours"): add `dark:text-slate-400`

Oil toggle buttons — the `applyOilToggleStyle` function in JS sets `style.cssText` directly. Dark variants on the Blade class won't apply. This will be handled in Task 6.

Empty state text:
```html
<p id="oilPricesEmpty" class="hidden text-center text-sm text-slate-400 mt-4">
```
becomes:
```html
<p id="oilPricesEmpty" class="hidden text-center text-sm text-slate-400 dark:text-slate-500 mt-4">
```

- [ ] **Step 6: Build and verify**

```bash
npm run build
```

Open the dashboard in browser, toggle dark mode. All cards, headings, labels, and filter controls should respond. Charts will still show in default colors — that's fixed in Task 6.

- [ ] **Step 7: Commit**

```bash
git add resources/views/livewire/dashboard.blade.php
git commit -m "feat: dark variants on dashboard static elements"
```

---

### Task 6: Dashboard Chart.js dark mode

**Files:**
- Modify: `resources/views/livewire/dashboard.blade.php` (the `@script` block only)

- [ ] **Step 1: Add `getChartTheme()` helper at the top of the `@script` block**

At the very beginning of the `@script` block (before the `PALETTE` constant), insert:

```js
function getChartTheme() {
    const dark = document.documentElement.classList.contains('dark');
    return {
        gridColor:    dark ? 'rgba(255,255,255,0.06)' : 'rgba(15,23,42,0.05)',
        tickColor:    dark ? '#475569' : '#475569',
        legendColor:  dark ? '#94a3b8' : '#64748b',
        yTitleColor:  dark ? '#94a3b8' : '#334155',
        tooltipBg:    'rgba(2,6,23,0.94)',
        tooltipTitle: '#e2e8f0',
        tooltipBody:  '#94a3b8',
        pieBorder:    dark ? '#0f172a' : '#ffffff',
        legendText:   dark ? '#94a3b8' : '#64748b',
        legendMuted:  dark ? '#475569' : '#94a3b8',
        legendCount:  dark ? '#334155' : '#cbd5e1',
    };
}
```

- [ ] **Step 2: Apply initial theme to fuel trends chart options**

In the `new Chart(canvas, { ... })` call for `chartFuelTrends`, update the static colour values to use `getChartTheme()`:

```js
const t = getChartTheme();

const chart = new Chart(canvas, {
    type: 'line',
    data: { labels: initial.labels, datasets: [] },
    options: {
        responsive:          true,
        maintainAspectRatio: false,
        animation: { duration: 800, easing: 'easeInOutQuart' },
        interaction: { mode: 'index', intersect: false },
        plugins: {
            legend: {
                position: 'top',
                labels: {
                    color:         t.legendColor,
                    usePointStyle: true,
                    pointStyle:    'circle',
                    padding:       22,
                    font: { size: 12, weight: '600', family: 'system-ui, sans-serif' },
                },
            },
            tooltip: {
                backgroundColor: t.tooltipBg,
                titleColor:      t.tooltipTitle,
                bodyColor:       t.tooltipBody,
                borderColor:     'rgba(99,102,241,0.35)',
                borderWidth:     1,
                padding:         14,
                cornerRadius:    12,
                callbacks: {
                    label: c => ` ${c.dataset.label}: ${c.parsed.y !== null ? c.parsed.y.toFixed(1) + ' ¢/L' : 'N/A'}`,
                },
            },
        },
        scales: {
            x: {
                ticks: { color: t.tickColor, maxTicksLimit: 9, font: { size: 11 } },
                grid:   { color: t.gridColor },
                border: { display: false },
            },
            y: {
                ticks: { color: t.tickColor, font: { size: 11 }, callback: v => v + ' ¢' },
                grid:   { color: t.gridColor },
                border: { display: false },
                title:  { display: true, text: 'Avg Price (¢/L)', color: t.yTitleColor, font: { size: 11 } },
            },
        },
    },
    plugins: [],
});
```

- [ ] **Step 3: Apply initial theme to brand share chart**

In the `new Chart(shareCanvas, { ... })` for the doughnut, update:

```js
const brandShareChart = new Chart(shareCanvas, {
    type: 'doughnut',
    data: {
        labels:   shareInitial.labels,
        datasets: [{
            data:            shareInitial.values,
            backgroundColor: shareInitial.labels.map((_, i) => PIE_PALETTE[i % PIE_PALETTE.length]),
            borderColor:     getChartTheme().pieBorder,
            borderWidth:     2,
            hoverOffset:     8,
        }],
    },
    options: {
        responsive:          true,
        maintainAspectRatio: false,
        cutout:              '62%',
        animation:           { duration: 800, easing: 'easeInOutQuart' },
        plugins: {
            legend: { display: false },
            tooltip: {
                backgroundColor: getChartTheme().tooltipBg,
                titleColor:      getChartTheme().tooltipTitle,
                bodyColor:       getChartTheme().tooltipBody,
                borderColor:     'rgba(99,102,241,0.35)',
                borderWidth:     1,
                padding:         14,
                cornerRadius:    12,
                callbacks: {
                    label: c => {
                        const count = shareInitial.counts[c.dataIndex] ?? '';
                        return ` ${c.label}: ${c.parsed}% (${count} sites)`;
                    },
                },
            },
        },
    },
});
```

- [ ] **Step 4: Update `buildLegend()` to use themed colours**

Replace the `buildLegend` function:

```js
function buildLegend(labels, values, counts) {
    const t = getChartTheme();
    shareLegendEl.innerHTML = labels.map((label, i) => `
        <div class="flex items-center gap-1.5 whitespace-nowrap">
            <span class="inline-block w-2.5 h-2.5 rounded-full flex-shrink-0"
                  style="background:${PIE_PALETTE[i % PIE_PALETTE.length]}"></span>
            <span style="font-weight:600;color:${t.legendText}">${label}</span>
            <span style="color:${t.legendMuted}">${values[i]}%</span>
            <span style="color:${t.legendCount};font-size:0.75rem">(${counts[i]})</span>
        </div>
    `).join('');
}
```

- [ ] **Step 5: Hoist `oilChartRef` and `applyOilToggleStyle` so the observer can reach them**

Before `initOilChart()`, declare:

```js
let oilChartRef   = null;
let oilToggleBtns = null;

function applyOilToggleStyle(btn, active) {
    const colour = OIL_COLOURS[btn.dataset.code];
    const dark   = document.documentElement.classList.contains('dark');
    btn.style.cssText = active
        ? `background:${colour}1a;border-color:${colour};color:${colour}`
        : dark
            ? 'background:#1e293b;border-color:#334155;color:#64748b'
            : 'background:#f8fafc;border-color:#e2e8f0;color:#94a3b8';
}
```

Inside `initOilChart()`, remove the existing `applyOilToggleStyle` function definition (it's now at module scope). Also assign `oilChartRef` and `oilToggleBtns` after creating the chart:

```js
async function initOilChart() {
    // ... fetch and guard logic unchanged ...

    const canvas = document.getElementById('chartOilPrices');
    const ctx    = canvas.getContext('2d');
    const t      = getChartTheme();

    oilChartRef = new Chart(ctx, {
        type: 'line',
        data: { labels: data.dates, datasets: buildOilDatasets(data.series) },
        options: {
            responsive:          true,
            maintainAspectRatio: false,
            animation:           { duration: 600 },
            interaction:         { mode: 'index', intersect: false },
            plugins: {
                legend: { display: false },
                tooltip: {
                    backgroundColor: t.tooltipBg,
                    titleColor:      t.tooltipTitle,
                    bodyColor:       t.tooltipBody,
                    borderColor:     'rgba(99,102,241,0.35)',
                    borderWidth:     1,
                    padding:         14,
                    cornerRadius:    12,
                    callbacks: {
                        label: c => ` ${c.dataset.label}: $${c.parsed.y !== null ? c.parsed.y.toFixed(2) : '—'}`,
                    },
                },
            },
            scales: {
                x: {
                    ticks: {
                        maxTicksLimit: 8,
                        color: t.tickColor,
                        font: { size: 11 },
                        callback: function(v) {
                            const label = this.getLabelForValue(v);
                            const d = new Date(label);
                            return isNaN(d.getTime()) ? label : d.toLocaleString('en-AU', { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit', hour12: false });
                        },
                    },
                    grid: { display: false },
                },
                y: {
                    ticks: { color: t.tickColor, font: { size: 11 }, callback: v => `$${(+v).toFixed(2)}` },
                    grid:  { color: t.gridColor },
                },
            },
        },
    });

    oilToggleBtns = document.querySelectorAll('#oilToggles .oil-toggle');
    oilToggleBtns.forEach(btn => {
        applyOilToggleStyle(btn, btn.dataset.code === OIL_ACTIVE_CODE);
        btn.addEventListener('click', () => {
            if (OIL_ACTIVE_CODE === btn.dataset.code) return;
            OIL_ACTIVE_CODE = btn.dataset.code;
            const activeLabel = OIL_LABELS[OIL_ACTIVE_CODE] ?? OIL_ACTIVE_CODE;
            oilChartRef.data.datasets.forEach(d => { d.hidden = d.label !== activeLabel; });
            oilChartRef.update();
            oilToggleBtns.forEach(b => applyOilToggleStyle(b, b.dataset.code === OIL_ACTIVE_CODE));
        });
    });
}
```

- [ ] **Step 6: Add MutationObserver to sync all charts on theme change**

After `initOilChart()` is called, append:

```js
new MutationObserver(() => {
    const t = getChartTheme();

    // Fuel trends chart
    chart.options.plugins.legend.labels.color             = t.legendColor;
    chart.options.plugins.tooltip.backgroundColor         = t.tooltipBg;
    chart.options.plugins.tooltip.titleColor              = t.tooltipTitle;
    chart.options.plugins.tooltip.bodyColor               = t.tooltipBody;
    chart.options.scales.x.ticks.color                   = t.tickColor;
    chart.options.scales.x.grid.color                    = t.gridColor;
    chart.options.scales.y.ticks.color                   = t.tickColor;
    chart.options.scales.y.grid.color                    = t.gridColor;
    chart.options.scales.y.title.color                   = t.yTitleColor;
    chart.update('none');

    // Brand share chart
    brandShareChart.data.datasets[0].borderColor          = t.pieBorder;
    brandShareChart.options.plugins.tooltip.backgroundColor = t.tooltipBg;
    brandShareChart.update('none');
    buildLegend(
        brandShareChart.data.labels,
        brandShareChart.data.datasets[0].data,
        shareInitial.counts
    );

    // Oil chart (may not be initialised yet)
    if (oilChartRef) {
        oilChartRef.options.scales.x.ticks.color         = t.tickColor;
        oilChartRef.options.scales.y.ticks.color         = t.tickColor;
        oilChartRef.options.scales.y.grid.color          = t.gridColor;
        oilChartRef.options.plugins.tooltip.backgroundColor = t.tooltipBg;
        oilChartRef.update('none');
    }

    // Oil toggle buttons
    if (oilToggleBtns) {
        oilToggleBtns.forEach(b => applyOilToggleStyle(b, b.dataset.code === OIL_ACTIVE_CODE));
    }
}).observe(document.documentElement, { attributeFilter: ['class'] });
```

- [ ] **Step 7: Build and verify**

```bash
npm run build
```

Open the dashboard. Toggle dark mode. Verify:
- All three charts update grid/tick/tooltip colours immediately
- Pie chart segment borders turn dark
- Brand legend text uses muted dark colours
- Oil toggle buttons update their inline styles
- No console errors

- [ ] **Step 8: Commit**

```bash
git add resources/views/livewire/dashboard.blade.php
git commit -m "feat: Chart.js dark mode via MutationObserver"
```

---

### Task 7: About page dark variants

**Files:**
- Modify: `resources/views/about.blade.php`

The About page uses `gray-` class names (not `slate-`).

- [ ] **Step 1: Apply dark variants throughout**

Card wrappers — change every `bg-white rounded-2xl border border-gray-100 shadow-sm` to:
```html
bg-white dark:bg-slate-900 rounded-2xl border border-gray-100 dark:border-slate-700 shadow-sm
```

Hero indigo header (`bg-indigo-600` section) — no change needed, keeps the colour in dark mode.

Feature grid text:
```html
<h3 class="text-sm font-bold text-gray-900 mb-1">
```
→
```html
<h3 class="text-sm font-bold text-gray-900 dark:text-slate-100 mb-1">
```

```html
<p class="text-xs text-gray-500 leading-relaxed">
```
→
```html
<p class="text-xs text-gray-500 dark:text-slate-400 leading-relaxed">
```

Feature icon wrappers (e.g. `bg-indigo-50`, `bg-teal-50`, `bg-amber-50`) — optionally add `dark:bg-indigo-950`, `dark:bg-teal-950`, `dark:bg-amber-950` respectively.

Data source notice boxes — amber box:
```html
<div class="bg-amber-50 border border-amber-100 rounded-2xl px-6 py-4 flex gap-4 items-start">
    <p class="text-xs text-amber-800 leading-relaxed">
```
→
```html
<div class="bg-amber-50 dark:bg-amber-950/30 border border-amber-100 dark:border-amber-900/50 rounded-2xl px-6 py-4 flex gap-4 items-start">
    <p class="text-xs text-amber-800 dark:text-amber-300 leading-relaxed">
```

Blue box similarly:
```html
<div class="bg-blue-50 dark:bg-blue-950/30 border border-blue-100 dark:border-blue-900/50 rounded-2xl px-6 py-4 flex gap-4 items-start">
    <p class="text-xs text-blue-800 dark:text-blue-300 leading-relaxed">
```

Developer card dividers and text:
```html
<div class="mt-auto pt-5 border-t border-gray-100">
    <p class="text-[11px] text-gray-400 leading-relaxed">
```
→
```html
<div class="mt-auto pt-5 border-t border-gray-100 dark:border-slate-700">
    <p class="text-[11px] text-gray-400 dark:text-slate-500 leading-relaxed">
```

Section headings (`text-xs font-bold text-gray-400 uppercase tracking-widest`):
→ add `dark:text-slate-500`

Developer name/role:
```html
<p class="text-sm font-bold text-gray-900">Roberto</p>
<p class="text-xs text-gray-400">Lead Developer</p>
```
→
```html
<p class="text-sm font-bold text-gray-900 dark:text-slate-100">Roberto</p>
<p class="text-xs text-gray-400 dark:text-slate-500">Lead Developer</p>
```

Email link:
```html
<a ... class="flex items-center gap-2.5 text-xs text-gray-500 hover:text-indigo-600 transition-colors group">
    <div class="w-7 h-7 rounded-lg bg-gray-100 group-hover:bg-indigo-50 ...">
```
→
```html
<a ... class="flex items-center gap-2.5 text-xs text-gray-500 dark:text-slate-400 hover:text-indigo-600 transition-colors group">
    <div class="w-7 h-7 rounded-lg bg-gray-100 dark:bg-slate-800 group-hover:bg-indigo-50 ...">
```

Feedback form card — same wrapper pattern.

- [ ] **Step 2: Verify in browser**

Navigate to `/about`, toggle dark mode. All cards, text, and notice boxes should respond.

- [ ] **Step 3: Commit**

```bash
git add resources/views/about.blade.php
git commit -m "feat: dark variants on About page"
```

---

### Task 8: Fuel site pages dark variants

**Files:**
- Modify: `resources/views/livewire/search.blade.php`
- Modify: `resources/views/fuelSite/show.blade.php`

- [ ] **Step 1: Search component dark variants**

In `resources/views/livewire/search.blade.php`:

Search input:
```html
class="w-full pl-11 pr-12 py-3.5 bg-white border border-gray-200 rounded-2xl shadow-sm
       text-sm text-gray-900 placeholder-gray-400
       focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-transparent
       transition-shadow duration-150"
```
→
```html
class="w-full pl-11 pr-12 py-3.5 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-2xl shadow-sm
       text-sm text-gray-900 dark:text-slate-100 placeholder-gray-400 dark:placeholder-slate-500
       focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-transparent
       transition-shadow duration-150"
```

Meta row text:
```html
<p class="text-sm text-gray-500">
    <span class="font-semibold text-gray-900">
```
→
```html
<p class="text-sm text-gray-500 dark:text-slate-400">
    <span class="font-semibold text-gray-900 dark:text-slate-100">
```

Empty state:
```html
<div class="w-14 h-14 rounded-2xl bg-gray-100 flex items-center justify-center mx-auto mb-4">
    <svg class="w-7 h-7 text-gray-300" ...>
</div>
<p class="text-sm font-semibold text-gray-900 mb-1">No stations found</p>
<p class="text-xs text-gray-400">Try a different name or address</p>
```
→
```html
<div class="w-14 h-14 rounded-2xl bg-gray-100 dark:bg-slate-800 flex items-center justify-center mx-auto mb-4">
    <svg class="w-7 h-7 text-gray-300 dark:text-slate-600" ...>
</div>
<p class="text-sm font-semibold text-gray-900 dark:text-slate-100 mb-1">No stations found</p>
<p class="text-xs text-gray-400 dark:text-slate-500">Try a different name or address</p>
```

Station cards:
```html
class="group flex items-center gap-4 bg-white rounded-2xl border border-gray-100 shadow-sm p-4
       hover:border-indigo-200 hover:shadow-md hover:-translate-y-px
       transition-all duration-200 cursor-pointer"
```
→
```html
class="group flex items-center gap-4 bg-white dark:bg-slate-900 rounded-2xl border border-gray-100 dark:border-slate-700 shadow-sm p-4
       hover:border-indigo-200 hover:shadow-md hover:-translate-y-px
       transition-all duration-200 cursor-pointer"
```

Station name:
```html
class="text-sm font-bold text-gray-900 truncate group-hover:text-indigo-600 transition-colors duration-150"
```
→
```html
class="text-sm font-bold text-gray-900 dark:text-slate-100 truncate group-hover:text-indigo-600 transition-colors duration-150"
```

Station address/city:
```html
class="text-xs text-gray-400 truncate mt-0.5"
class="text-[11px] text-gray-400"
```
→ add `dark:text-slate-500` to each.

- [ ] **Step 2: Fuel site show page dark variants**

In `resources/views/fuelSite/show.blade.php`:

Breadcrumb:
```html
class="inline-flex items-center gap-1.5 text-xs text-gray-400 hover:text-indigo-500 ..."
```
→ add `dark:text-slate-500`

Hero card:
```html
class="bg-white rounded-2xl overflow-hidden shadow-sm border border-slate-200"
```
→ add `dark:bg-slate-900 dark:border-slate-700`

Station name and address text (`text-slate-900`, `text-slate-500`):
→ add `dark:text-slate-100` and `dark:text-slate-400` respectively.

Brand badge:
```html
class="text-[10px] font-bold text-indigo-600 uppercase tracking-widest bg-indigo-50 border border-indigo-200 px-2 py-0.5 rounded-full"
```
→ add `dark:bg-indigo-950 dark:border-indigo-900`

"Open in Maps" button:
```html
class="... bg-slate-50 hover:bg-indigo-50 border border-slate-200 text-slate-600 hover:text-indigo-600 ..."
```
→ add `dark:bg-slate-800 dark:border-slate-700 dark:text-slate-300`

Section headings (`text-xs font-bold text-gray-400`): add `dark:text-slate-500`

Current price cards:
```html
class="bg-white rounded-2xl border border-gray-100 shadow-sm p-4 relative overflow-hidden"
```
→ add `dark:bg-slate-900 dark:border-slate-700`

Price values (`text-gray-900`): add `dark:text-slate-100`
Price timestamps (`text-gray-400`): add `dark:text-slate-500`

Price history table wrapper:
```html
class="bg-white rounded-2xl border border-gray-100 shadow-sm overflow-hidden"
```
→ add `dark:bg-slate-900 dark:border-slate-700`

Table header row border:
```html
class="border-b border-gray-100"
```
→ add `dark:border-slate-700`

Table header text (`text-gray-400`): add `dark:text-slate-500`

Table rows:
```html
class="hover:bg-gray-50/60 transition-colors duration-100"
```
→ add `dark:hover:bg-slate-800/60`

Table body divider:
```html
class="divide-y divide-gray-50"
```
→ add `dark:divide-slate-700/50`

Date cells (`text-gray-500`): add `dark:text-slate-400`
Price cells (`text-gray-900`): add `dark:text-slate-100`
Price unit span (`text-gray-400`): add `dark:text-slate-500`

- [ ] **Step 3: Build and verify**

```bash
npm run build
```

Navigate to `/fuel`, toggle dark mode. Cards, search input, and list items should all respond. Navigate to a fuel site detail page and verify the hero card, price cards, and history table all respond.

- [ ] **Step 4: Commit**

```bash
git add resources/views/livewire/search.blade.php resources/views/fuelSite/show.blade.php
git commit -m "feat: dark variants on fuel site list and detail pages"
```

---

### Task 9: Fuel map dark variants

**Files:**
- Modify: `resources/views/livewire/fuel-map.blade.php`

The map itself (Google Maps tiles) is excluded per spec. This task covers the overlay UI elements — the search bar, info panels, and any visible backgrounds.

- [ ] **Step 1: Read the full fuel-map.blade.php**

Read `resources/views/livewire/fuel-map.blade.php` in full to identify all `bg-white`, `text-gray-`, `border-gray-`, `bg-slate-` class usages in the overlay elements (search bar, filter bar, price pin styles are CSS not Tailwind).

- [ ] **Step 2: Apply dark variants to map overlay elements**

For each overlay element (search input wrapper, autocomplete container styles, price filter bar if present):

Search bar wrapper (e.g. `bg-white/90 backdrop-blur`):
→ add `dark:bg-slate-900/90`

Search input:
→ add `dark:bg-slate-900 dark:border-slate-700 dark:text-slate-100 dark:placeholder-slate-500`

Any panel or card overlays over the map:
→ add `dark:bg-slate-900 dark:border-slate-700` and `dark:text-slate-100` / `dark:text-slate-400` as appropriate.

`.pac-container` CSS (Google Places autocomplete dropdown) — add dark styles:
```css
html.dark .pac-container {
    background: rgba(15,23,42,0.97);
    border-color: #334155;
}
html.dark .pac-item {
    color: #94a3b8;
    border-color: #1e293b;
}
html.dark .pac-item:hover,
html.dark .pac-item-selected { background: #1e293b; }
html.dark .pac-item-query { color: #f1f5f9; }
```

- [ ] **Step 3: Build and verify**

```bash
npm run build
```

Navigate to `/` (map page), toggle dark mode. Search bar and any overlay panels should darken. The map tiles themselves stay light (expected, per spec).

- [ ] **Step 4: Commit**

```bash
git add resources/views/livewire/fuel-map.blade.php
git commit -m "feat: dark variants on fuel map overlay elements"
```
